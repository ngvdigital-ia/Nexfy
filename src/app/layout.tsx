import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: {
    default: "NexFy - Gateway de Pagamentos",
    template: "%s | NexFy",
  },
  description: "Plataforma de pagamentos com checkout otimizado. Apple Pay, Google Pay, PIX e cartao.",
  keywords: ["pagamentos", "checkout", "stripe", "pix", "gateway"],
  authors: [{ name: "NGV Digital" }],
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
    apple: "/favicon.svg",
  },
  openGraph: {
    title: "NexFy - Gateway de Pagamentos",
    description: "Plataforma de pagamentos com checkout otimizado",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
